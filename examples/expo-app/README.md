# Example Expo app

This folder is reserved for a future minimal Expo sample (Drizzle + Zustand) that installs the two workspace packages.

For now, follow the root [README](../../README.md) and wire `setupBridge` in your own app.
